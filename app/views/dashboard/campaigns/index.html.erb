<div class="col-md-12">
  <%- if @campaigns.empty? %>
  <div class="row">
    <div class="col-md-offset-3 col-md-6 empty-state text-center">
      <h2><%= t 'campaigns.new_user.title' %></h2>
      <p><%= t 'campaigns.new_user.description' %></p>
      <br />
      <a class="btn btn-default btn-primary btn-lg" href="<%= new_campaign_path %>">
        <%= fa_icon "plus-circle" %>
        <%= t 'campaigns.new' %>
      </a>
    </div>
  </div>
  <% else %>
  <section class="panel">
    <header class="panel-heading">
      <%= t 'routes.campaigns' %>
    </header>
    <div class="panel-body panel-index-body">
      <table class="table table-striped index-table">
        <tbody>
          <% @campaigns.each do |campaign| %>
            <tr>
              <td style="background-image: url(//img.youtube.com/vi/<%= campaign.engagement_player.try(:decorate).try(:youtube_video_id) %>/maxresdefault.jpg)">
                <div class="button-row">
                  <a href="<%= campaign_path(campaign) %>" class="btn btn-primary">
                    <%= fa_icon "search" %>
                  </a>
                  <a href="<%= campaign_build_path(id: t('wicked.basic'), campaign_id: campaign.id) %>" class="btn btn-primary">
                    <%= fa_icon "pencil" %>
                  </a>
                  <%= link_to fa_icon("trash-o"), campaign, confirm: 'Are you sure you want to delete this campaign?', method: :delete, class: 'btn btn-danger' %>
                </div>
                <div class="pull-left cover">
                  <h3><%= campaign.name %></h3>
                  <small class="text-muted">
                    <%= t 'campaigns.created_on', date: campaign.created_at.strftime("%m/%d/%Y") %>
                    &middot;
                    <%= t 'campaigns.updated_on', time: time_ago_in_words(campaign.updated_at) %>
                  </small>
                  <br />
                  <%- unless campaign.engagement_player.blank? %>
                    <span class="label label-primary"><%= t 'routes.engagement_player' %></span>
                  <% end -%>
                  <%- unless campaign.share.blank? || !campaign.share.enabled? %>
                    <span class="label label-primary"><%= t 'routes.share' %></span>
                  <% end -%>
                  <%- unless campaign.community.blank? || !campaign.community.enabled? %>
                    <span class="label label-primary"><%= t 'routes.community' %></span>
                  <% end -%>
                  <%- unless campaign.alternate_locales.empty? %>
                    <span class="label label-primary"><%= t 'routes.translations' %></span>
                  <% end -%>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="panel-footer clearfix">
      <div class="pull-right">
        <a class="btn btn-default btn-primary" href="<%= new_campaign_path %>">
          <%= fa_icon "plus-circle" %>
          <%= t 'campaigns.new' %>
        </a>
      </div>
    </div>
  </section>
  <% end -%>
</div>